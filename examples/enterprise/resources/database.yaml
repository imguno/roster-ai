kind: resource
name: database
type: api
actions:
  query:
    description: "Run SQL queries"
  logs:
    description: "View query logs and slow queries"
  migrate:
    description: "Run database migrations"
watch:
  - backend-team
  - analytics-team
