kind: resource
name: github
type: api
actions:
  read:
    description: "Read repos, issues, PRs"
  write:
    description: "Push code, create branches"
  pr:
    description: "Create and manage pull requests"
